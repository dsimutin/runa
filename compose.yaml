services:
  bot:
    build:
      context: .
    image: runa-bot:local
    restart: unless-stopped
    env_file:
      - .env
    environment:
      WEBHOOK_URL: ""
    init: true
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
